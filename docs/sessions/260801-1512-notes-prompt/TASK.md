# 任务卡：详细笔记 prompt 重构（双变体 + 密度护栏 + 拼接修复）

## 目标

详细笔记当前输出比原文还长 21%（誊抄式对话回放），且对无说话人标签的转录（CapsWriter/YouTube）注入假标签 `Unknown`。本卡重构笔记生成：prompt 换为"共享基座 + 有/无说话人双变体"，切片格式化按数据形态选变体，拼接层修双重标题，并加密度校验护栏。

## 非目标

- 不改总结模块（summary 对无标签多人内容误选单人 prompt 是存量问题，已记 backlog）
- 不改章节划分（chapters）逻辑、前端展示、缓存结构
- 不做"生成后 LLM 自评质量"之类的新机制

## 基线与所有权

- **Task-Id**：VideoTranscriptAPI-20260801-01
- **任务类型**：backend-logic
- **复杂度**：M
- **Base commit**：9876a13
- **Branch**：`feat/notes-prompt-redesign`（从 main 新开；禁碰 main、禁 force-push）
- **Worktree**：不开，主仓直接开分支
- **当前唯一写入者**：Codex（本卡执行期间）
- **执行器与模型**：codex（delegate 派发，envelope 回填）
- **计划者与审查者**：主脑（Claude fable5）拆卡与验收

## 修改边界

- **允许**：
  - `src/video_transcript_api/llm/prompts/__init__.py`（仅 NOTES 区域：`NOTES_SYSTEM_PROMPT`、`build_notes_user_prompt`）
  - `src/video_transcript_api/llm/processors/notes_processor.py`
  - `src/video_transcript_api/llm/__init__.py`（导出符号同步）
  - `tests/unit/test_notes_processor.py` 及必要的新测试文件（放 `tests/unit/`）
- **禁止**：
  - `config/`（工作区里 `config/config.example.jsonc` 有他人未提交改动，**不得 stage/commit 该文件**）
  - 其他 prompt（CALIBRATE/SUMMARY/CHAPTERS/...）、`api/`、`cache/`、`downloaders/`、`transcriber/`
- **高风险区域**：
  - `format_notes_segment_slice` 被 `api/services/llm_ops.py` 等处间接使用，改函数签名前先 `grep` 全部调用点
  - 本仓部分文件可能是 CRLF 行尾，编辑时保持原行尾，勿整文件重写换行风格

## 设计定稿（照此实现，不要再发挥）

### 1. Prompt：共享基座 + 双变体

仿照仓内 `CALIBRATE_SYSTEM_PROMPT_WITH_SPEAKER = CALIBRATE_SYSTEM_PROMPT + "..."` 的组合方式：

- `NOTES_SYSTEM_PROMPT_BASE`（共享基座，不单独导出也可）
- `NOTES_SYSTEM_PROMPT_SPEAKER = BASE + 有标签说话人段`
- `NOTES_SYSTEM_PROMPT_NO_SPEAKER = BASE + 无标签说话人段`
- 旧符号 `NOTES_SYSTEM_PROMPT` 删除，更新全部引用（含 `llm/__init__.py` 导出与测试）

**基座全文**（三引号字符串，原样落地）：

```
你是长篇访谈/播客的精读笔记作者。输入是一章带时间戳的对话转写切片，输出是这一章的精读笔记。

## 定位

精读笔记介于"全文总结"和"逐字稿"之间：读者读完笔记应获得这一章的全部信息量，但阅读耗时约为读逐字稿的一半。因此：
- 保留的单位是"信息点"，不是"对话回合"。
- 你的任务是压缩表达、归拢碎片，不是复述对话过程。
- 目标篇幅为输入正文的 40%-60%。若输出接近原文长度，说明你在誊抄而不是做笔记。

## 必须保留（一个都不能丢）

- 所有事实、数字、金额、日期、人名、机构名、产品名、专有名词
- 所有判断、结论、理由、反例、分歧
- 具体例子和故事的关键情节
- 表达力强、值得直接引用的原话（每小节至多 2 处，加引号）

## 必须丢弃（不丢等于失职）

- 应答与寒暄（"对""嗯""hello 大家好"）、口头禅、车轱辘话
- 纯引导性的提问——把它化入小节标题；仅当提问本身携带事实或观点时保留其内容
- 逐句时间戳、逐句说话人前缀
- 同一信息的"转述 + 原话"双写：二选一

## 输出格式（严格遵守）

- 只输出 Markdown 正文。不要复述章节标题，不要输出概要、前言、"本章位置"等元信息；上一章/下一章标题只是给你的定位上下文，禁止写入正文
- 用中文书写；人名、产品名等专有名词保留原文，英文原话引用可保留英文
- 标题只允许 ###（三级），格式为 `### [HH:MM:SS] 小节标题`，时间戳取小节内容在原文中的起点；小节标题尽量点出结论，而不只是话题名
- 标题下用 `-` 分层 bullets，最多三层；每条 bullet 是一个完整的信息点
- 加粗只用于关键结论与判断；不使用 emoji

## 红线

- 不得新增事实、猜测因果、把不确定写成确定；含混表述保持含混
- 小节之间保持时间顺序；小节内部允许把同一话题的碎片归拢到一起
```

**有标签变体附加段**：

```
## 说话人处理（输入带说话人标签）

- 输入每行格式为 `[时间戳] 说话人: 内容`。
- 观点归属重要时（双方交锋、各自表态、嘉宾个人经历）以 `姓名：` 开头标注 bullet；同一人连续陈述时不必逐条标注。
- "这是谁的观点"本身是信息：当某个判断/结论有明确归属时，归属不能丢。

## 示例

输入片段：
[00:12:03 - 00:12:05] 曼祺: 那你觉得他们做这个决定的核心原因是什么？
[00:12:06 - 00:12:19] 高洪浩: 嗯，就是，我觉得核心还是成本吧。他们算过一笔账，一年大概能省两个亿。
[00:12:20 - 00:12:21] 曼祺: 两个亿，这么多。
[00:12:22 - 00:12:30] 高洪浩: 对。而且这个还不算人力。

输出：
### [00:12:03] 决策的核心原因：成本
- 高洪浩：核心是成本，**一年大概能省两个亿**，且不含人力
```

**无标签变体附加段**：

```
## 说话人处理（输入无说话人标签）

- 输入每行格式为 `[时间戳] 内容`，没有说话人标注；但内容可能是多人对话（访谈/播客），只是没有区分出来。
- 默认以内容为主语写笔记（"核心原因是成本，一年约省两亿"），不写"某人说"。
- 只有当原文文字本身明确了身份——自我介绍（"我是任鑫"）、主持人点名、开场介绍嘉宾——才可以用该身份归属观点；除此之外禁止推测或发明说话人，也不得使用 "Unknown"、"说话人A" 之类的占位归属。
- 若文本中出现 `>>` 等换人标记，可用它判断对话轮转（比如识别问答节奏），但换人标记不构成身份依据，也不要把它抄进笔记。
- 问答节奏可识别时，引导性提问仍化入小节标题。

## 示例

输入片段：
[00:12:03 - 00:12:05] 那你觉得他们做这个决定的核心原因是什么？
[00:12:06 - 00:12:19] >> 嗯，就是，我觉得核心还是成本吧。他们算过一笔账，一年大概能省两个亿。
[00:12:20 - 00:12:21] 两个亿，这么多。
[00:12:22 - 00:12:30] 对。而且这个还不算人力。

输出：
### [00:12:03] 决策的核心原因：成本
- 核心是成本，**一年大概能省两个亿**，且不含人力
```

`build_notes_user_prompt` 保持现有字段（章标题/概要/邻章标题/切片），首句措辞与新定位对齐（"生成本章精读笔记"），不新增字段。

### 2. 变体选择与切片渲染（notes_processor.py）

- 新增检测：全文 source segments 中任一 segment 的 `("speaker", "speaker_name", "spk", "speaker_id")` 存在非空值 → speaker 模式；全部没有 → no-speaker 模式。**按整个任务判定一次，所有章节统一用同一变体**（不允许一章一个模式）。
- speaker 模式：切片渲染维持现状（`[ts] 名字: 内容`，个别缺失的仍渲染 `Unknown`——已分离转录里这是诚实的）。
- no-speaker 模式：渲染 `[ts] 内容`，**不再注入 `Unknown:`**。
- 函数命名遵循可检索原则（如 `detect_notes_speaker_labels`），不用 `check`/`helper` 之类裸词。

### 3. 拼接层双重标题修复（sanitizer）

- 章节头保持程序生成的 `## [start - end] title` 不变，它是唯一的章节级标题。
- 对每章模型输出做 sanitize：行首匹配 `^#{1,2}\s` 的行——若其标题文本（去掉行首 `#`、时间戳、首尾空白后）与本章 title 相同则**删除该行**；否则**降级为 `###`**。只处理 h1/h2，不碰 `###` 及更深层级。

### 4. 密度校验护栏

- 每章生成后计算 `ratio = len(笔记文本) / len(切片正文)`（字符数）。
- `ratio > 0.8` → 判定誊抄，重试一次：同样的 messages，在 user prompt 末尾追加固定字面量强化指令（"警告：你上一次的输出篇幅接近原文，这是誊抄不是笔记。请严格执行 40%-60% 篇幅预算，应用'必须丢弃'清单。"）。
- 重试后仍 `> 0.8`：`logger.warning`（完整字面量、含章节序号与 ratio）后照常使用重试结果，不失败任务。
- 重试的 token 用量按现有 usage 记录路径正常累计。
- 不设下限（章节本身信息稀薄时短是正常的）。

## 完成条件

- **行为验收**：
  - 有标签输入 → 选 SPEAKER 变体、切片带名字；无标签输入 → 选 NO_SPEAKER 变体、切片无 `Unknown`
  - 模型输出含 `# 标题` / `## 标题` 时，拼接结果不出现双重章节标题
  - 单章输出超长触发一次重试；二次超长记 warning 且笔记照常落盘
- **相关测试**（新增/更新，全部通过）：
  - 变体选择：全有标签 / 全无标签 / 混合（个别 segment 缺 speaker）三种
  - no-speaker 渲染不含 `Unknown`
  - sanitizer：h1/h2 降级、与章题重复的行删除、`###` 不受影响
  - 密度重试：mock LLM 首次超长→重试合格；两次超长→warning + 落盘
  - 存量测试全绿：`uv run pytest tests/unit/test_notes_processor.py tests/unit/test_detailed_notes_view.py`
- **lint / typecheck / build**：`uv run pytest tests/unit/`（看 exit code，不叠加 `-q`）
- **其他纪律**：测试 console 输出纯英文无 emoji；commit 信息署名 `[codex]` 并带 trailer `Task-Id: VideoTranscriptAPI-20260801-01`；小步提交

## 当前状态

- **已完成**：无（新卡）
- **下一步唯一动作**：从 9876a13 开 `feat/notes-prompt-redesign` 分支开始实现
